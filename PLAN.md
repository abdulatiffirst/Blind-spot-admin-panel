## Plan

1. Reproduce import failure.
2. Validate dependency presence in installed modules.
3. Apply and verify minimal fix.
## Plan

1. Reproduce the import-resolution error from the build tool.
2. Determine whether `styled-components` is missing from `node_modules` or there is a resolver/config mismatch.
3. Apply the smallest fix and verify by re-running the command.
# Plan

1. Install Firebase package.
2. Add `src/firebase/config.js` with app + analytics initialization.
3. Import config module in app bootstrap.
4. Check lints for updated files.
# IELTS Admin Panel Build Plan

1. Install required dependencies.
2. Scaffold required folder/file structure.
3. Build theme/global styles and app entry wiring.
4. Implement Firebase config, auth, and Firestore/storage CRUD with verification.
5. Build auth and passages hooks plus protected routing.
6. Build UI components and layout shell.
7. Implement all pages and feature modules.
8. Add environment template and run lint fixes.
